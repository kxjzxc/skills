# ADR Format

ADRs live in `docs/adr/` and use sequential numbering: `0001-slug.md`.

```md
# {Short title}

{1-3 sentences describing the context, decision, and why.}
```

Create an ADR only when the decision is hard to reverse, surprising without context, and the result of a real trade-off.
